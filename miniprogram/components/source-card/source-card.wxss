@import "../../app.wxss";

.source-card {
  display: flex;
  align-items: center;
  gap: 18rpx;
  padding: 22rpx;
  text-align: left;
}

.source-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 76rpx;
  height: 76rpx;
  border-radius: 50%;
  background: #eef6ea;
  color: #6f985a;
  font-size: 30rpx;
  font-weight: 700;
}

.source-main {
  flex: 1;
  min-width: 0;
}

.source-title {
  color: #2f2a25;
  font-size: 28rpx;
  font-weight: 700;
}

.source-summary {
  margin-top: 8rpx;
  color: #756d66;
  font-size: 24rpx;
  line-height: 1.4;
}

.row-arrow {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 46rpx;
  height: 46rpx;
  border-radius: 50%;
  background: #f4f1ec;
  color: #9a938b;
  font-size: 32rpx;
  line-height: 1;
  flex: none;
}
